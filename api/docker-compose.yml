
services:
  postgres:
    image: postgres:15-alpine
    container_name: omnisport-postgres
    environment:
      POSTGRES_DB: omnisport
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secretpassword
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: